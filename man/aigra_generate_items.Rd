% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aigra-api.R, R/aigra-user-api.R
\name{aigra_generate_items}
\alias{aigra_generate_items}
\title{Generate and review assessment items}
\usage{
aigra_generate_items(
  data,
  model = "gemini-3.1-pro-preview",
  provider = "auto",
  gemini.API = NULL,
  openai.API = NULL,
  groq.API = NULL,
  anthropic.API = NULL,
  backend_path = NULL,
  ...
)

aigra_generate_items(
  data,
  model = "gemini-3.1-pro-preview",
  provider = "auto",
  gemini.API = NULL,
  openai.API = NULL,
  groq.API = NULL,
  anthropic.API = NULL,
  backend_path = NULL,
  ...
)
}
\arguments{
\item{data}{A data frame with the standard AIGRA tabular columns.}

\item{model}{Model name to use.}

\item{provider}{LLM provider. Use \code{"auto"}, \code{"gemini"}, \code{"openai"},
\code{"groq"}, or \code{"anthropic"}, depending on the backend configuration.}

\item{gemini.API}{Optional Gemini API key.}

\item{openai.API}{Optional OpenAI API key.}

\item{groq.API}{Optional Groq API key.}

\item{anthropic.API}{Optional Anthropic API key.}

\item{backend_path}{Optional path to the AIGRA Python backend.}

\item{...}{Additional arguments passed to \code{\link[=aigra_generate_tabular_items]{aigra_generate_tabular_items()}}.}
}
\value{
A data frame if read_csv is TRUE; otherwise invisibly returns latest CSV path.

A data frame or list returned by the AIGRA tabular generation backend.
}
\description{
Runs the AIGRA Python backend pipeline to generate, solve, review,
and export assessment items.

This user-facing wrapper accepts an AIGRA tabular item bank that has already
been loaded into R as a data frame. To use CSV or XLSX files, first call
\code{aigra_read_template()} and pass the resulting data frame to this function.
}
\examples{
\dontrun{
template <- aigra_read_template("template.xlsx")

result <- aigra_generate_items(
  data = template,
  provider = "gemini",
  model = "gemini-2.5-flash",
  source_language = "English",
  target_language = "English",
  n_clones = 1,
  max_items = 1
)
}
}
